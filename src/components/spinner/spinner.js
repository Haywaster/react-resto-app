// Как будет выглядить сообщение загрузки данных для пользователя

import React from 'react';

const Spinner = () => {
    return <div style={{color:'white'}} className="spinner">loading...</div>
}

export default Spinner;